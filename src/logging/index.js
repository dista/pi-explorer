import { createLogger, initializeDefaultLogger, getDefaultLogger } from './logger.js';
export { createLogger, initializeDefaultLogger, getDefaultLogger };
export default {
  createLogger,
  initializeDefaultLogger,
  getDefaultLogger,
};