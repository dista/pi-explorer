import { createErrorHandler } from './errorHandler.js';
import { createRequestLogger } from './requestLogger.js';

export { createErrorHandler, createRequestLogger };

export default {
  createErrorHandler,
  createRequestLogger,
};